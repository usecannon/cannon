import { readIpfs, writeIpfs, isIpfsGateway } from './ipfs';

describe('ipfs.ts', () => {
  describe('isIpfsGateway()', () => {
    it('returns false when port is 5001', () => {

    });

    it('returns false when scheme is ipfs+http(s)', () => {

    });
  });

  describe('readIpfs()', () => {
    it('can download and decompress cannon info on gateway', async () => {

    });

    it('can download and decompress cannon info on ipfs api', async () => {

    });
  });

  describe('writeIpfs()', () => {
    it('throws exception on gateway', async () => {

    });

    it('uploads compressed archive that can be read back', async () => {

    });
  });
});
