import { ActionKinds, registerAction, getChainDefinitionValidator } from './actions';

describe('actions.ts', () => {
  describe('registerAction()', () => {
    it('does not allow redeclaration of step with same name', async () => {

    });

    it('registers action kind on success', async () => {

    });
  });

  describe('getChainDefinitionValidator()', () => {
    it('returns ajv validator', async () => {

    });
  });
});
