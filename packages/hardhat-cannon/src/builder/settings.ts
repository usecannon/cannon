import { ChainBuilderContext } from './';

// Declare a configurable string which may be set by downstream 
// Canonfiles or `deploy.json`. The option may be used in followin steps
export default function (ctx: ChainBuilderContext) {

}