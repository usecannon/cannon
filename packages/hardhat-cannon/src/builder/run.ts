import { ChainBuilderContext } from './';

// execute the custom script at the given location, supplying correct hre
// and other provider options
// needed to deploy whatever it is needs to be deployed
// need support for specifying command line options environment variables from context values
export default function (ctx: ChainBuilderContext, options: any) {

}