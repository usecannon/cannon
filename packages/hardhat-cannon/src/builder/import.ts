import { ChainBuilderContext } from './';

// load the specified chain state (downloading from IPFS if necessary), 
// or run a chain builder with sources if the chain with specified options not available
// settings may be specified in `options` which should be heeded, and trigger a chain build
export default function (ctx: ChainBuilderContext, options: any) {

}