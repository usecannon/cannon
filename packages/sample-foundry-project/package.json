{
  "name": "sample-foundry-project",
  "version": "1.1.1",
  "private": true,
  "scripts": {
    "build-cannonfile": "npx @usecannon/cli build cannonfile.toml",
    "run": "npx @usecannon/cli greeter",
    "deploy": "npx @usecannon/cli deploy greeter",
    "run-synthetix": "npx @usecannon/cli synthetix"
  },
  "devDependencies": {
    "@usecannon/cli": "../cli"
  }
}
