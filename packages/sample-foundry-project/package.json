{
  "name": "sample-foundry-project",
  "version": "1.1.0-dev.0",
  "private": true,
  "scripts": {
    "build": "npx cannon build",
    "run": "npx cannon greeter:1.0.0",
    "deploy": "npx cannon deploy greeter:1.0.0",
    "run-synthetix": "npx cannon synthetix"
  },
  "devDependencies": {
    "@usecannon/cli": "../cli"
  }
}
